# Task 8: Walk the service surfaces

Walked 2026-07-28 (server clock ET) against `localhost:3000`. Four short journeys plus their
empty states: incident reporting, IT support requests, the AVS generator, and notifications.

Personas: `dev.volunteer@yale.edu` (cleared, one incident report, one tech request, three unread
notifications) for the populated states, `dev.director@yale.edu` (cleared, zero rows in all three
tables, verified against the live database) for the empty states.

Surfaces walked: `/incidents` (filed a real report end to end), `/incidents/mine`,
`/incidents/[id]`, `/support/new` (submitted a real ticket), `/support`, `/support/[id]`,
`/clinic/avs` (generated both an English and a Spanish handout and decoded the PDF text),
the notification bell, and `/notifications`. Density and style claims are measured with
`browser_evaluate` against the live DOM at 1200x861, not eyeballed; the numbers are quoted inline.

Two things shaped the walk. `/clinic/avs` is gated on `clinic.access`, which no role in this
database grants, so neither audit persona can reach it (both land on `/no-access`); it was walked
as `j.carney@yale.edu`, the seeded Platform Admin whose `*` grant covers it. And the AVS is the
only surface here whose output leaves the building in a patient's hand, so it gets the most
findings.

## Findings

| id | surface | lens | severity | reach | what is wrong | concrete fix | effort |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F-08-1 | `/incidents`, section 10 "Your information" | ia | blocks | every person who files a report and wants to stay unnamed; the whole point of the anonymity control | The form never says who receives the report, and its one statement about disclosure is narrower than a reporter will read it. Walked: section 10 renders "Your name: Dev Volunteer" and a single checkbox labelled "I would prefer to remain anonymous (your name is not shared with the subject)" (`src/app/(app)/incidents/page.tsx:187-191`); I checked it and re-snapshotted, and nothing else on the page changed. What actually happens is that the report fans out to every holder of `incidents.manage` (`src/modules/incidents/services/report.ts:238`), the reporter's real name renders verbatim on the detail page each of them opens (`src/app/(app)/incidents/[id]/page.tsx:267`, which prints `report.reporter.name` unconditionally and puts the anonymity flag in a separate field beside it), and the review queue's search box is documented as matching "Subject, reporter, or report #" (`src/app/(app)/incidents/review/page.tsx:131`). So "anonymous" means anonymous to one named party, and the person deciding whether it is safe to report a colleague is told nothing about the set of people who will read their name. Nowhere on the form, the confirmation, or the detail page is that audience described. | Replace the bare checkbox with an explicit disclosure block above it: "Your report goes to the clinic's incident reviewers ({n} people who hold the incidents.manage permission). They will see your name whether or not you check the box below." Then relabel the checkbox to what it does: "Do not share my name with the person I am reporting". `peopleWithAnyPermission(["incidents.manage"])` is already called on submit, so the page can call it too and render the count (and, if ops wants, the role names). Mirror the same sentence on `/incidents/[id]` next to the Anonymity field so the reporter can re-read the promise later. | S |
| F-08-2 | `/incidents`, section 6 "ongoing risk" | flow | blocks | every reporter who does not scroll to section 6, on the field that drives escalation | The form pre-answers the safety question as "No" on the reporter's behalf. Measured on a clean page load: `input[name=immediateRisk][value=no]` is `checked: true` and `value=yes` is `checked: false`, from the `defaultChecked` at `src/app/(app)/incidents/page.tsx:152`. The action reads it as `immediateRisk: formData.get("immediateRisk") === "yes"` (`src/app/(app)/incidents/actions.ts:70`), so a reporter who never reaches section 6 silently submits "not time-sensitive". That flag is exactly what changes the reviewer alert: `report.immediateRisk` selects between "was submitted and flagged as an immediate risk" and plain "was submitted" in both the email and the Teams card (`src/modules/incidents/services/report.ts:259-263`). Section 6 sits well down a form measured at 1682px in an 861px viewport, so it is below the fold on arrival. The result is a form that quietly downgrades urgent reports to routine ones and tells the reviewer the reporter said so. | Drop `defaultChecked` from the "No" radio at `page.tsx:152` and make the question required, so nothing is submitted on the reporter's behalf. Add the third option the two-way choice is missing and the server already models as false: leave it unanswered rather than guessing. If ops wants a default, it must be the safe one ("Yes"), not the quiet one. | S |
| F-08-3 | `/incidents` form top | ia | costs-time | every reporter with something happening right now, which is the case the form is worst at | Nothing on the form says this is an asynchronous queue rather than a way to raise an alarm. Measured against the rendered page: `/911|emergency|urgent.*call|call .*immediately/i` matches nothing in `main`. The page description is "File a Professional Standards Incident Report. Anyone signed in may report a concern about anyone." (`src/app/(app)/incidents/page.tsx:93`), and the only urgency affordance is the section 6 radio, whose sole effect is one word in a reviewer's email. There is no stated review interval anywhere in the module. So a volunteer watching an unsafe handoff happen has a 24-field web form and no idea whether anyone will read it in the next hour or the next week. | Add an `Alert tone="warning"` immediately under the PageHeader, before the form: "This form is reviewed during clinic business hours and is not monitored in real time. If someone is in immediate danger, call 911. If a patient is at risk right now, escalate to the attending or the Executive Director on duty before filing." Resolve the escalation contact from `getSupportContact()` / a new ops setting rather than hardcoding it, the way the login page already resolves its IT contact. | S |
| F-08-4 | `/incidents/mine` and `/incidents/[id]` after submit | flow | costs-time | every reporter, on the surface they will revisit to find out what happened | The app tells the reporter what it stored and nothing about what happens next. Walked end to end: submitting redirected to `/incidents/mine?submitted=2` with the banner reading exactly "Report #2 submitted." (`src/app/(app)/incidents/mine/page.tsx:74`), and the detail page's entire "Reporting details" block is "Reported by / Anonymity / Submitted / Strike requests". No owner, no expected turnaround, no statement that they will be contacted, and no route to add information or withdraw the report. The app does in fact notify the reporter when a report is resolved or dismissed (`notifyReporterOfResolution`, `src/modules/incidents/services/report.ts:345-373`), and does not notify on the `UNDER_REVIEW` transition, so a reporter watching the status badge sees nothing move for the entire review and is never told to stop watching. | Say the two things already true. In the success banner: "Report #2 submitted. The clinic's incident reviewers have been notified. You will get a notification when the report is resolved or dismissed." On `/incidents/[id]`, add a "What happens next" line under the status badge with the same sentence, plus the support contact for anything the reporter needs to add, since there is no in-app follow-up path. Both are static copy over facts the services already guarantee. | S |
| F-08-5 | `/clinic/avs`, Español output | flow | blocks | every Spanish-speaking patient handed a summary, which is a large share of a free clinic's panel | The Spanish handout is bilingual in the worst place: the scaffolding is Spanish and every word the clinician actually wrote is English. I filled a realistic visit, switched "Summary language" to Español, generated, and decoded the PDF's text. It reads: "INFORMACIÓN DEL PACIENTE", "DETALLES DE LA VISITA", "MOTIVO DE LA VISITA / Hypertension follow-up", "DIAGNÓSTICOS / AFECCIONES / High blood pressure, Type 2 diabetes", "NOTAS / Take your blood pressure at home every morning before breakfast and write the numbers down.", "SEGUIMIENTO / 3 meses, Blood pressure check", "RECOMENDACIONES DE ESTILO DE VIDA / Cut back on salt. Walk 20 minutes a day." The cause is that `buildSummary` localizes headings, chip labels and dates but passes every free-text field through untouched (`src/modules/clinic/avs/build-summary.ts:62-64` and `:86`, plus the follow-up note join at `:76-81`). Nothing in the UI warns about this: the language `Select` is unlabelled as to scope and the three "Printed as typed" hints speak to verbatim, not to language. So the patient carries home a document that looks translated and whose only actionable sentences are not. | Make the boundary visible where the text is typed. When `preferredLang === "es"`, render an inline note under each free-text field ("Printed as typed. Type this in Spanish; only the headings are translated.") and change the three hint strings from "Printed as typed." to "Printed as typed, in whatever language you type." Add a one-line `Alert tone="warning"` under the language `Select` when `es` is chosen, stating that headings and dates are translated and everything typed is printed as entered. The real fix is a preview (F-08-6), which makes the mixture visible instead of described; ship the copy now and the preview next. | S |
| F-08-6 | `/clinic/avs`, "Generate PDF" | flow | blocks | every clinician producing a handout, every visit, and every patient who receives a wrong one | There is no way to see the handout before it is printed and handed over. `grep -n "preview"` in `src/modules/clinic/avs/avs-tool.tsx` returns nothing; the only output path is `handleGenerate` building a blob and clicking a synthetic `<a download>` (`avs-tool.tsx:83-95`). Walked twice: clicking Generate produced a silent file download with no on-screen change at all (measured immediately after: zero elements matching `[role="alert"], [role="status"]`, and the button label back to "Generate PDF"). Switching the language changed nothing on screen either, measured: after selecting Español the entire form, including every field label, every chip and all eight follow-up options, still rendered in English. So the clinician's only route to checking a patient handout is to download it, find it, open it in another application, and read it, per patient, and the one thing they most need to check (F-08-5's language mixture, F-08-7's English footer) is invisible until they do. | Render the summary on the page instead of only into the PDF. `buildSummary(data, data.preferredLang)` is already a pure function returning a `LocalizedSummary` of `blocks`/`items`, so a `<AvsPreview summary={...} />` component can render the same structure into a right-hand column (or a collapsible panel under the form) live as the clinician types, using the same headings and localized labels the PDF uses. This makes the language boundary self-evident, removes the download-to-check loop, and needs no change to `build-summary.ts` or `avs-pdf.tsx`. | M |
| F-08-7 | `/clinic/avs` PDF footer | visual | costs-time | every Spanish-language handout | The last line of the Spanish handout is in English. Decoded from the generated `es` PDF: the disclaimer correctly reads "Este resumen es para sus registros y no es un expediente médico completo. Comuníquese con la clínica si tiene preguntas." and is immediately followed by "Questions? Contact HAVEN Free Clinic at hfc.it@yale.edu." That sentence is a hardcoded English literal (`src/modules/clinic/avs/avs-pdf.tsx:179-182`), the only string in the document that does not come from the `STRINGS[lang]` table every other label goes through. | Add `footerContact` to both language entries in `src/modules/clinic/avs/strings.ts` as a template ("¿Preguntas? Comuníquese con {org} en {contact}." / "Questions? Contact {org} at {contact}."), surface it on `LocalizedSummary` in `buildSummary`, and interpolate it at `avs-pdf.tsx:181` instead of the literal. | S |
| F-08-8 | `/clinic/avs` PDF footer contact | ia | costs-time | every patient who reads the handout and tries to use the address on it | The patient handout prints the clinic's internal IT help mailbox as the patient's contact. Observed in the generated PDF: "Questions? Contact HAVEN Free Clinic at hfc.it@yale.edu." That value is `branding.supportEmail` (`src/app/(app)/clinic/avs/page.tsx:5-11`), the same setting `getSupportContact()` resolves and labels "Contact the {orgName} IT team" (`src/platform/branding/support.ts:12-18`), which I confirmed on `/login` as `mailto:hfc.it@yale.edu`. That helper's own doc comment scopes it to "signed-out pages (sign-in, 404, welcome)", so the AVS is reusing an internal IT address well outside its intent. One setting now serves two audiences with opposite needs, and a patient with a question about their diagnoses is pointed at the address that handles password resets. | Split the setting. Add `branding.patientContact` to the settings registry (label "Patient-facing contact", hint "Printed on patient handouts such as the After Visit Summary") and read that in `src/app/(app)/clinic/avs/page.tsx` instead of `branding.supportEmail`, falling back to the org's main clinic phone or line rather than to the IT address. Leave `branding.supportEmail` as the internal contact everywhere else. | S |
| F-08-9 | `/clinic/avs`, download filename | flow | costs-time | any clinician producing more than one handout in a session, and anyone doing an English and Spanish pair | Two different handouts download under one filename. Observed: generating in English produced `AVS-Garcia-2026-07-25.pdf`; switching to Español and generating again produced `AVS-Garcia-2026-07-25.pdf`, which replaced the first file in the download directory. The template is `AVS-${lastName}-${visitDate}.pdf` (`src/modules/clinic/avs/avs-tool.tsx:92`), carrying neither the language nor any disambiguator. A clinician who wants to hand a patient the Spanish copy and file the English one has two identically named files, or one, and no way to tell from the name which is which. | Extend the template at `avs-tool.tsx:92` to `AVS-${lastName}-${visitDate}-${data.preferredLang}.pdf`, and append a short time or counter suffix so a regenerated handout for the same patient and date does not silently replace the earlier one. | S |
| F-08-10 | `/clinic/avs`, whole form | flow | costs-time | every clinician interrupted mid-visit, which is the normal condition in a clinic | A refresh, a back button or a closed tab destroys the whole handout with no warning. The tool holds every field in a `useReducer` over `initialAvsData` with no persistence of any kind (`src/modules/clinic/avs/form-state.ts:3`, `avs-tool.tsx:33`), and the page description says so plainly: "Nothing is saved." Measured on the live page: `typeof window.onbeforeunload` is `"object"` (null), so no unload guard is installed, against a form measured at 2406px of scroll height. The team clearly thought about accidental loss in one place, arming "Clear / New summary" so it only wipes on a second click within 3s (`avs-tool.tsx:38-63`), but the far more likely loss (a navigation on a shared clinic laptop) is unguarded. | Add a `beforeunload` guard that arms whenever any field differs from `initialAvsData`, so the browser's own confirm dialog catches a refresh or a close. It is a small `useEffect` in `AvsTool` over the existing `data` object and needs no new state. If ops wants more, mirror the same dirty check into a `sessionStorage` draft keyed to nothing patient-identifying and restore it on mount, but the unload guard alone closes the common case. | S |
| F-08-11 | `/support`, `/support/[id]`, notification bell, "Awaiting requester" | ia | costs-time | every requester whose ticket is ever blocked on them, on the state designed to unblock it | When a ticket is waiting on the volunteer, every in-app surface says so in the third person, and the one that pings them does not say it at all. Walked the whole loop: as the Platform Admin I set my own submitted ticket #2 to "Awaiting requester", then signed back in as `dev.volunteer`. `/support` shows the row as "Awaiting requester" and `/support/cms5iu1e...` shows an "Awaiting requester" badge over a Conversation reading "No replies yet.", so the volunteer is given a third-person sentence about somebody called "the requester" and no call to action. The label is deliberately viewer-neutral and the source says why: "Viewer-neutral: this label renders on manager surfaces too (triage list, status dropdown), where 'Awaiting you' would wrongly read as awaiting the manager" (`src/modules/support/components/status-badge.tsx:20-22`). `setStatus` does notify (`src/modules/support/services/manage.ts:185-207`), but the in-app row it creates carries none of the information: `notify()` builds the inbox row's title and body from the Teams payload (`src/platform/notifications/notify.ts:92-94`), which `setStatus` passes as `title: "IT Support #N update"` and `summary: updated.subject` (`manage.ts:203`). Verified in the database after the transition: the row reads "IT Support #2 update" / "Duo push not arriving on my phone", with no status anywhere in it. The email template is the only artifact that names the state, and it repeats the same third person ("is now Awaiting requester", `src/platform/email/templates/support.ts:104`). So the volunteer gets a ping that says nothing, and three surfaces that say the ticket awaits a person they must work out is themselves. | Two changes. (1) Make the badge viewer-aware: give `SupportStatusBadge` a `viewer: "requester" | "manager"` prop and render `AWAITING_REQUESTER` as "Awaiting your reply" when `viewer === "requester"`. Both call sites already know which they are (`RequestList` takes `showRequester`, `TicketDetail` takes `isRequester`), so it is a prop thread, not new state; on the requester's detail view pair it with a line pointing at the reply box below. (2) Put the status in the notification: pass `title: `IT Support #${updated.number} is now ${STATUS_LABELS[status]}`` and a `summary` naming the action when the new status is `AWAITING_REQUESTER` ("IT needs a reply from you before they can continue.") at `manage.ts:203`, so the bell row is self-explanatory. `STATUS_LABELS` is already imported there for the email. | M |
| F-08-12 | `/support/[id]` header | visual | polish | every requester reading their own ticket in any non-initial state | The header prints the word "Submitted" twice, meaning two different things, six words apart. Observed on ticket #2 while it was in the awaiting state: the sub-header read "#2 · DUO MFA · Submitted Jul 28, 2026" immediately above a status badge reading "Awaiting requester", and while the ticket was in its initial state the same two elements both read "Submitted". So the reader has to work out that the first "Submitted" is a date verb and the second is the lifecycle state. | Relabel the sub-header's date to name what it is: "#2 · DUO MFA · Opened Jul 28, 2026", leaving "Submitted" to mean only the status. One string. | S |
| F-08-13 | Notification bell and `/notifications` | visual | costs-time | every user with a mixed read/unread list, which is everyone after their first week | Unread is signalled by a small dot and nothing else, and the dot's presence knocks the list's left edge out of alignment. Measured on the bell dropdown across all four rows: read and unread share an identical class string (`flex w-full flex-col items-start gap-0.5 border-b border-border-subtle px-4 py-2.5 ...`), an identical transparent background, and an identical title (`text-sm font-medium text-foreground`, computed weight 500). The only difference is a 6x6px `bg-brand` dot (`src/platform/ui/notification-bell.tsx:146`), which pushes unread titles to x=814 against x=800 for the read one. `/notifications` repeats the pattern one size up: an 8px dot (`src/app/(app)/notifications/page.tsx:73`) with the same identical backgrounds and weight 500 titles, titles at x=81 unread against x=65 read, and the word "Unread" present only as `sr-only` text (`page.tsx:74`), so it is never visible. Scanning a list for a 6px dot is the whole unread affordance in both places, and the ragged left edge makes the list read as misaligned rather than as two states. | Give unread a treatment that survives a glance and keeps the two states aligned. Render the dot slot at a fixed width for every row (an invisible spacer on read rows) so titles share one left edge, tint the unread row (`bg-brand/5`), and set the unread title to `font-semibold` against `font-normal text-muted-foreground` for read. Apply the same three rules in both `notification-bell.tsx:140-160` and `notifications/page.tsx:69-81` so the bell and the full page agree. | S |
| F-08-14 | `/notifications` | ia | costs-time | every user who clicks "View all" from a bell showing unread, once the list outgrows one page | Clicking through from the bell reshuffles the list and drops the only way to find the unread ones. Observed with four notifications: the bell listed them as IT Support #2, "We received your IT request", "You are on the schedule this Saturday", "Your HIPAA certificate is on file"; clicking "View all" produced IT Support #2, "Your HIPAA certificate" (read), "We received your IT request" (unread), "You are on the schedule" (read). The reorder is deliberate and the services disagree by design: the bell sorts unread-first (`orderBy: [{ readAt: { sort: "desc", nulls: "first" } }, ...]`, `src/platform/notifications/inbox.ts:50`) with a comment explaining that this keeps the dropdown honest against the unread badge, while `listNotifications` for the page sorts strictly by `createdAt` (`inbox.ts:64`). The page has no unread filter and no tabs (its only control is "Mark all as read", `src/app/(app)/notifications/page.tsx:56-58`) and pages at `NOTIFICATIONS_PAGE_SIZE`, so a user who followed the badge to deal with three unread items lands on a chronological page where those items are scattered among read ones, identifiable only by F-08-13's dot. | Carry the bell's promise onto the page. Add an "Unread only" filter (a `?unread=1` param threaded into `listNotifications`' `where` as `readAt: null`, with a count in the label) rendered as a pair of NavForm tabs beside "Mark all as read", and default the page to the bell's ordering by reusing the same `orderBy` array from `inbox.ts:50` so read and unread do not interleave unless the user asks for strict chronology. | S |
| F-08-15 | `/notifications` empty state | flow | polish | every user on their first visit, and every user of a quiet account | The first-run screen offers a live control that does nothing. Walked as `dev.director` with zero notifications: the page renders "Mark all as read" as an enabled button above the text "No notifications yet." The button is rendered unconditionally at `src/app/(app)/notifications/page.tsx:56-58`, above the `rows.length === 0` branch at `:60`, and its action runs `updateMany({ where: { personId, readAt: null } })` (`src/platform/notifications/inbox.ts:88`), which matches nothing, reports nothing and leaves the page identical. The bell's own dropdown gets this right, hiding its "Mark all as read" when the list is empty. | Move the `<form action={markAllAction}>` inside the non-empty branch, or gate it on `rows.some((n) => !n.readAt)` so it only appears when there is something to mark. One conditional at `page.tsx:56`. | S |
| F-08-16 | `/support`, `/incidents/mine`, `/notifications` empty states | visual | polish | every new volunteer, on their first pass through the hub | The three empty states of one release are three different components saying three different amounts. Walked all three as `dev.director` with genuinely zero rows. `/incidents/mine` is the good one: a centered column with `mt-12` spacing, "You have not filed any incident reports.", and a primary "Report a concern" button (`src/app/(app)/incidents/mine/page.tsx:78-84`). `/support` is a padded `Card` containing the four words "No requests yet." and no action at all, so the only route onward is the module sub-tab (`src/modules/support/components/request-list.tsx:28-34`). `/notifications` is a bare `<p className="text-sm text-muted-foreground">No notifications yet.</p>` with no card, no centering and no action (`src/app/(app)/notifications/page.tsx:61`). The module a volunteer is most likely to hit first with nothing in it (support) is the one with no way forward. | Standardize on the `/incidents/mine` shape, ideally as a shared `EmptyState` primitive taking a message, an optional explanation and an optional action. Give `/support` the missing CTA ("Submit a request" -> `/support/new`) plus a line on what the module is for, and give `/notifications` the same centered treatment with an explanatory line ("Shift reminders, request updates and compliance notices will appear here.") since it has no action to offer. | S |

## Needs its own brainstorm

No `L` findings. F-08-6 (the AVS preview) is the largest item at `M` and is bounded: `buildSummary`
is already a pure function over a serializable `LocalizedSummary`, so rendering it to the page
reuses the existing model rather than requiring a new one. F-08-11 is `M` only because it pairs a
prop thread with a new notification, both following patterns the support module already has.

## Coverage notes

**What I could not walk.**

- **`/clinic/avs` as either audit persona.** The module declares `accessPermission: "clinic.access"` (`src/platform/modules/registry.ts:112`) and no role in this database grants it: querying `RoleGrant` for `clinic.access` returns zero rows, and `dev.volunteer` and `dev.director` hold no `RoleAssignment` at all. Both land on `/no-access` ("Your current role doesn't include this page. If you think you should have access, ask a clinic admin to update your permissions."), which is the intended behavior for a nobody-by-default grantable permission and not a defect. The AVS findings were therefore walked as `j.carney@yale.edu`, the seeded Platform Admin whose `*` grant covers `clinic.access`. That is the right surface but the wrong hands: I could not observe how a front-desk volunteer discovers the tool, because in this database none of them can open it. Whether the AVS should be reachable by the people actually running clinic is an ops question, not a UX one, so no finding is filed on the gate itself.
- **The AVS PDF rendered visually.** I decoded the generated PDF's text streams to confirm the exact Spanish/English mixture quoted in F-08-5 and F-08-7, which is stronger evidence than a screenshot for a language claim, but I did not render the page image. Layout, wrapping and how the mixture *looks* on paper are unexamined; the finding rests on content, not typesetting.
- **Attachments on either form.** Both `/incidents` (section 9) and `/support/new` accept files and both mirror the server's size cap in the browser (`IncidentAttachmentsField`, `src/app/(app)/incidents/incident-attachments-field.tsx:16-29`). I read the guards but uploaded nothing, so the upload-error experience on these two forms is not covered here. Task 1 and Task 2 already walked upload failures on the recruitment and compliance surfaces.
- **The reviewer and manager sides.** `/incidents/review`, `/incidents/strikes`, `/support/all` and `/support/epic` are approver surfaces and belong to Tasks 9 and 10. I read `review/page.tsx` and `[id]/page.tsx` only far enough to establish what a *reporter's* name does once it reaches them (F-08-1), and read `manage.ts` only far enough to establish that `setStatus` sends no notification (F-08-11). The brief's known "Copy email" no-op in `epic-request-form.tsx` was not chased, per instruction.
- **A ticket receiving an actual reply.** I confirmed from source that a manager comment notifies the requester (`notifyCommentAdded`, `src/modules/support/services/comments.ts:143-149`) and that every notification row in this database carries a non-null link, but I did not post a manager reply and watch it arrive. The support conversation thread is therefore covered as a static surface, not as a live exchange.
- **Notification volume.** `dev.volunteer` had four notifications at the time of the walk (six afterwards, see below). F-08-13's and F-08-14's measurements are real but taken over those four rows; the claims about scanning cost and pagination are grounded in the measured absence of any filter control and in `NOTIFICATIONS_PAGE_SIZE` from the source, not in a walked long list.

**Fixture state consumed and left behind.**

- **Created and left in place: incident report #2**, filed by `dev.volunteer` against no linked person (free-text subject description only), concern type "Professional Conduct", `anonymous = true`, status `SUBMITTED`. It cannot be withdrawn through the UI (F-08-4), so it persists. Submitting it queued `incidents.report_submitted` notifications to every `incidents.manage` holder.
- **Created and left in place: tech request #2** ("Duo push not arriving on my phone", category `DUO_MFA`), filed by `dev.volunteer`, status `SUBMITTED`. Submitting it queued a `support.ticket_submitted` confirmation to `dev.volunteer` (visible in the bell as "IT Support #2 received") and `support.ticket_manager_alert` to every `support.manage_requests` holder.
- **Changed and reverted: tech request #2's status.** For F-08-11 I set it to `AWAITING_REQUESTER` as `j.carney`, read both requester-side surfaces, and set it back to `SUBMITTED`. Verified against the database afterwards: both tech requests read `SUBMITTED` and both incident reports read `SUBMITTED`. The round trip is not free, though: `setStatus` notifies on every transition, so it left **two unread `support.status_changed` notifications** on `dev.volunteer` (both titled "IT Support #2 update"), queued two emails, and recorded two `support.status_change` audit rows. `dev.volunteer` therefore now has six notifications rather than the fixture's four.
- **Read state on one notification.** Clicking "You are on the schedule this Saturday" in the bell marked it read, taking `dev.volunteer` from three unread to two. Not reverted.
- **AVS:** nothing persisted. The tool holds everything in component state and writes no rows ("Nothing is saved"); the two generated PDFs went to the session scratchpad.
- **No application code was modified.** Screenshots and the decoded PDFs live in the session scratchpad only. Playwright wrote three PNGs and a `.playwright-mcp/` directory into the worktree root during the walk; both were moved out and removed, and `git status --short` is empty apart from this fragment.
